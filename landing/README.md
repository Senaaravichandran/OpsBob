# OpsBob Landing

Standalone IBM Carbon themed landing page.

Open `index.html` directly in a browser. This folder is independent from the React frontend app and does not import files from `frontend/`.
